{
  "name": "xiaopi-homepage",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "npx --no-install astro dev",
    "build": "npx --no-install astro build",
    "preview": "npx --no-install astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.17.1"
  }
}
